rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /receipts/{tenant}/{receiptId} {
      allow read, write: if request.auth != null && request.auth.uid == request.resource.metadata.ownerUid;
    }
  }
}
